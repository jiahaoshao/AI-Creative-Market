spring:
        datasource:
                name: test
                url: jdbc:mysql://localhost:3306/test?characterEncoding=UTF8&autoReconnect=true&useSSL=false&&serverTimezone=GMT%2B8
                username: root
                password: fangyi
                driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
        mapper-locations: classpath:mapper/*.xml  #配置映射文件
        type-aliases-package: name.sauerkraut.acreativemarket.bean #配置实体类

server:
        port: 888
